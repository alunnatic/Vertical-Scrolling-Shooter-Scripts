using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Border : MonoBehaviour {
    /*
    public float leftSide;
    public float rightSide;
    public float bottom;
    public float top;
*/
	// Use this for initialization
	void Start () {
        /*
        leftSide = transform.position.z;
        rightSide = GetComponentInChildren<Border_Right>().transform.position.z;
        bottom = GetComponentInChildren<Border_Bottom>().transform.position.x;
        top = GetComponentInChildren<Border_Top>().transform.position.z;
        */
    }
	
}
